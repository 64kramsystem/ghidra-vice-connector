# Ghidra MCP repositories

- Also read `~/code/home_saverio_configs.shared/agent_docs.SKIP/AGENTS.git.md`.
- `$GHIDRA_INSTALL_DIR` identifies the installation; use `$GHIDRA_SOURCE_DIR` when source can replace guesswork.
- Record public-contract changes in `CHANGELOG.md`; breaking changes use `tools/release minor`.
